import { styled } from "styled-components";

export const Wrapper = styled.div`
  padding: 60px 0;
`;

Wrapper.CardsWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  gap: 40px;
  justify-content: center;
  margin: 50px 0;
`;
